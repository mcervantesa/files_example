springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html
springdoc.paths-to-match=/test/**, /files/**, /xyz/**

file.openapi.dev-url=http://localhost:8080
file.openapi.prod-url=https://example-api.com

## MongoDB
spring.data.mongodb.database=files_fb
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017

# create and drop table, good for testing, production set to none or comment it
spring.jpa.hibernate.ddl-auto=create-drop

spring.servlet.multipart.max-file-size=10GB
spring.servlet.multipart.max-request-size=10GB


## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB

## File Storage Properties
file.upload-dir= C:/uploads


files.app.jwtSecret= ======================filesApiSpring===========================
files.app.jwtExpirationMs= 60000